<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portal 2B - DevSistemas</title>
    
    <style>
        :root {
            --bg: #0d1117;
            --card: #161b22;
            --text: #c9d1d9;
            --accent: #238636;
            --accent-hover: #2ea043;
            --border: #30363d;
            --footer-text: #8b949e;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            display: flex;
            flex-direction: column; /* Organiza em coluna para o rodapé ficar embaixo */
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container { width: 100%; max-width: 500px; padding: 20px; transition: 0.3s; flex: 1; display: flex; align-items: center; justify-content: center; }
        .hidden { display: none !important; }

        .card {
            background: var(--card);
            padding: 2.5rem;
            border-radius: 12px;
            border: 1px solid var(--border);
            text-align: center;
            box-shadow: 0 8px 24px rgba(0,0,0,0.5);
            width: 100%;
        }

        h2 { margin-bottom: 0.5rem; color: #fff; }
        input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            background: #0d1117;
            border: 1px solid var(--border);
            color: white;
            border-radius: 6px;
            box-sizing: border-box;
        }

        button {
            width: 100%;
            background: var(--accent);
            color: white;
            border: none;
            padding: 12px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 600;
            margin-top: 10px;
        }

        /* Estilo do Rodapé */
        footer {
            width: 100%;
            padding: 20px 0;
            text-align: center;
            border-top: 1px solid var(--border);
            background: var(--card);
            color: var(--footer-text);
            font-size: 14px;
            letter-spacing: 1px;
        }

        .turma-tag {
            color: #58a6ff;
            font-weight: bold;
            text-shadow: 0 0 8px rgba(88, 166, 255, 0.3);
        }

        /* Painel Layout */
        #painel-container { max-width: 800px; }
        .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin: 20px 0; }
        .card-painel { background: var(--bg); padding: 15px; border-radius: 8px; border: 1px solid var(--border); text-align: left; }
    </style>
</head>
<body>

    <!-- TELA DE LOGIN -->
    <div id="login-container" class="container">
        <div class="card">
            <h2>💻 DevClass Login</h2>
            <p style="color: #8b949e;">Acesse o projeto do sistema</p>
            <input type="text" id="user" placeholder="Usuário">
            <input type="password" id="pass" placeholder="Senha">
            <button onclick="fazerLogin()">Entrar</button>
            <p id="erro" style="color: #f85149; font-size: 14px; margin-top: 10px;"></p>
        </div>
    </div>

    <!-- PAINEL -->
    <div id="painel-container" class="container hidden">
        <div class="card">
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <h2 style="margin: 0;">Dashboard Alpha</h2>
                <button onclick="logout()" style="width: auto; padding: 5px 15px; background: transparent; border: 1px solid #f85149; color: #f85149;">Sair</button>
            </div>
            
            <div class="grid">
                <div class="card-painel">
                    <h3 style="margin-top:0">🛠️ Repositório</h3>
                    <a href="https://github.com" target="_blank" style="color: #58a6ff; text-decoration: none;">Acessar GitHub →</a>
                </div>
                <div class="card-painel">
                    <h3 style="margin-top:0">📊 Status</h3>
                    <p style="margin:0; color: #3fb950;">Online</p>
                </div>
            </div>
        </div>
    </div>

    <!-- RODAPÉ DA TURMA -->
    <footer>
        &copy; 2024 - <span class="turma-tag">2B DESENVOLVIMENTO DE SISTEMAS</span>
    </footer>

    <script>
        function fazerLogin() {
            const user = document.getElementById('user').value;
            const pass = document.getElementById('pass').value;
            if (user.toLowerCase() === "admin" && pass === "123") {
                document.getElementById('login-container').classList.add('hidden');
                document.getElementById('painel-container').classList.remove('hidden');
            } else {
                document.getElementById('erro').innerText = "Usuário ou senha incorretos!";
            }
        }

        function logout() { window.location.reload(); }
    </script>
</body>
</html>
